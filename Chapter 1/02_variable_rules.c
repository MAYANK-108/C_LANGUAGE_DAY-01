#include<stdio.h>

int main(){
    int a;
    int b;
    int c;
    int Mayank; // this is a different variable
    int mayank; // and this is also a different from Mayank
}
    // rules to name variables
    // 1. First character must be an alphabet or underscore(_)
    // 2. No commas or blanks are allowed
    // 3. No special symbols other than (_) are allowed
    // 4. Variable names are case sensitive
    // you should create meaningfull variable names it enhances the readablity of the program
    

    // here are some reserved words which can not be used as variables
    // auto      double     int      struct
    // break     long       else     switch
    // case      return     enum     typedef
    // char      register   extern   union
    // const     short      float    unsigned
    // continue  signed     for      void
    // default   sizeof     goto     volatile
    // do        static     if       while