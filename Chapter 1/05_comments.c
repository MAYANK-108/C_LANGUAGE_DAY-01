#include <stdio.h>
int main() {
   printf("hello world");
   // all lines starting from two (/) are used to type plain language the compiler(gcc) ignores these lines

   // this is a single line comment

   /* 
   And this is a multi line comment
   */
}

